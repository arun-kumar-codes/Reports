# Optional: if set, the AI summary endpoint will call the OpenAI API.
# Leave blank to use the deterministic local mock summary generator.
OPENAI_API_KEY=

# Optional model override (defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
